private _return = false;
private _linked = [];

/*
if (player call OT_fnc_hasWeaponEquipped) exitWith {true};

_linked = ([player, false] call DEDF_fnc_unitEquippedItems) select 0;

{
	private _cls = _x;
	if ()
}foreach(_linked);
*/
_return